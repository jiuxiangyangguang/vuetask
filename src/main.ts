import { createApp } from 'vue'
import App from './App.vue'
import vuetask from 'vuetask'
import 'vuetask/dist/css/index.css'

createApp(App).use(vuetask).mount('#app')

// createApp(App).mount('#app')
